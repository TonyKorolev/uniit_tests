package seminars.third.hw_interval_tdd;

public class Interval {
    public boolean numberInInerval(int i) {
        if (i > 25 && i < 100) {
            return true;
        } else {
            return false;
        }
    }
}
